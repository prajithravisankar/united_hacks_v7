namespace Boys.Ledger.Domain;

// B01 placeholder: exists only to prove the Domain project + test harness wiring.
// Real domain logic (ledger, state machine, settlement) arrives in Epic E3.
public static class Arithmetic
{
    public static int Add(int a, int b) => a + b;
}
